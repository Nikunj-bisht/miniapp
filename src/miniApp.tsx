import {StyleSheet, Text, View} from 'react-native';

const MiniApp = () => {
  return (
    <View>
      <Text style={{fontWeight: '800', fontSize: 40, color: 'red'}}>
        Mini App
      </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default MiniApp;
